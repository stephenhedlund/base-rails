<!DOCTYPE html>
<html>
  <head>
    <title>Base Rails</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.css">

  </head>
    
    <% if controller_name != 'home' || (action_name != 'login' && action_name != 'signup')%>
    <nav>
      <ul>
        <li>
          <a href="/home">Discover</a>
        </li>
        <li>
          <a href="/home">Bookmarks</a>
        </li>
        <li>
          <a href="/home">Visits</a>
        </li>
        <li>
          <a href="/home">Profile</a>
        </li>
      </ul>
     </nav>
     <% end %> 

  <body>
    <%= yield %>
  </body>

</html>
